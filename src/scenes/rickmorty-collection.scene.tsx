import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterCollectionContainer } from 'pods/character-collection';

export const RickMortyCollectionScene = () => (
  <AppLayout>
    <CharacterCollectionContainer />
  </AppLayout>
);
